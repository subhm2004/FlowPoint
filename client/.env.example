# Backend API base URL — MUST end with /api when server uses BASE_PATH=/api (default).
# Local backend: http://localhost:8000/api
# Render:       https://your-service.onrender.com/api
VITE_API_BASE_URL=http://localhost:8000/api
